import type { MetadataRoute } from 'next'
export default function sitemap():MetadataRoute.Sitemap { const base=process.env.NEXT_PUBLIC_SITE_URL||'https://dengine.vercel.app'; return ['','/browse','/custom','/pricing','/about','/contact','/imprint','/privacy','/terms'].map((path,i)=>({url:`${base}${path}`,lastModified:new Date(),changeFrequency:i===0?'weekly':'monthly',priority:i===0?1:path==='/custom'?0.9:0.6})) }
